@page "/employee"
@page "/employee/{id:int}"
@inject IEmployeeService EmployeeService
@using AVSBiro.Shared

@if (Id == null)
{
    <PageTitle>Create a new Super Hero</PageTitle>
    <h3>Create a new Super Hero</h3>
}
else
{
    <PageTitle>Edit @employee.FirstName </PageTitle>
    <h3>Edit @employee.FirstName </h3>
}

@*<EditForm Model="employee" OnSubmit="HandleSubmit">
    <div>
        <label for="firstname">First name</label>
        <InputText id="firstname" @bind-Value="employee.FirstName" class="form-control"></InputText>
    </div>
    <div>
        <label for="lirstname">Lirst name</label>
        <InputText id="lirstname" @bind-Value="employee.LastName" class="form-control"></InputText>
    </div>
    <div>
        <label for="position">Position</label>
        <InputText id="position" @bind-Value="employee.Position" class="form-control"></InputText>
    </div>
    <div>
        <label>Position</label>
        <InputSelect @bind-Value="employee.Position.Id" class="form-select">
            @foreach(var position in EmployeeService.Positions)
            {
                <option value="@position.Id">@position.Name </option>
            }
        </InputSelect>
    </div>
    <br />
    <button type="submit" class="btn btn-primary">@btnText</button>
</EditForm>*@

<EditForm Model="employees" OnSubmit="HandleSubmit">
    <div>
        <label for="firstname">First Name</label>
        <InputText id="firstname" @bind-Value="employees.FirstName" class="form-control"></InputText>
    </div>
    <div>
        <label for="lastname">Last Name</label>
        <InputText id="lastname" @bind-Value="employee.LastName" class="form-control"></InputText>
    </div>
    <div>
        <label>Position</label><br />
        <InputSelect @bind-Value="employee.Position" class="form-select">
            @foreach (var position in EmployeeService.Positions)
            {
                <option value="@position.Id">@position.Name</option>
            }
        </InputSelect>
    </div>
    <br />
    <button type="submit" class="btn btn-primary">@btnText</button>
</EditForm>

@*Forma ne dohvaća zaposlenika pravilno, pokušati s bazom. 59.*@


@code {
    [Parameter]
    public int? Id { get; set; }

    string btnText = string.Empty;

    //used full path because Blazor declared class Employee from page namespace
    //instead of shared entity namespace (Employee employee = new Employee(); - doesn't work )
    AVSBiro.Shared.Employee employee = new AVSBiro.Shared.Employee();

    protected override async Task OnInitializedAsync()
    {
        btnText = Id == null ? "Save new employee" : "Update employee";
        await EmployeeService.GetPositions();
    }

    protected override async Task OnParametersSetAsync()
    {
        if (Id == null)
        {
            // crt
        }
        else
        {
            employee = await EmployeeService.GetSingleEmployee((int)Id);
        }
    }

    async Task HandleSubmit()
    {

    }



}


